# 技术路线图 / Roadmap

> 里程碑、已完成能力、以及下一步方向。配套:[ARCHITECTURE.md](ARCHITECTURE.md)、[PROGRESS.md](PROGRESS.md)。
> 状态截至 2026-08。图例:✅ 已完成 · 🚧 进行中 · 🔜 计划 · 🅿️ 已搁置(附理由)。

---

## 里程碑概览

| 里程碑 | 主题 | 状态 |
|---|---|---|
| **M1** | 决策核心:量化筛选 + LLM 委员会 + 结构化决策 | ✅ |
| **M2** | 服务端风控闸门 + 模拟盘 + 四种模式 | ✅ |
| **M3** | 安全加固:mode-唯一真相、伪造 as_of 防护、熔断、token/CSRF | ✅ |
| **M4** | Web UI 全量 + 记忆/知识系统 + 业绩追踪 | ✅ |
| **M5** | 评测体系:决策记分卡 + 前瞻收益 + 历史回放 + 跨区间验证 | ✅ |
| **M6** | 看盘/推荐体验增强(regime、荐股、自选、缓存、可展开理由) | ✅ |
| **M7** | 实盘接入(富途 OpenD,默认关闭,硬门) | ✅(适配器就绪) |
| **M8** | 学习闭环通电 + 评测(机制✅;置信度信号不显著;复盘→行为无响应) | 🟡 机制完成 |
| **M9** | 让委员会真正**权衡**自己的复盘(瓶颈是框定不是数据,被 M8 重新定义) | 🔜 |

---

## 已完成能力(M1–M7)

### 决策与执行
- ✅ 量化筛选器(趋势/动量/量能)→ 候选;`POST /signals/run`、`GET /signals`。
- ✅ 四角色 LLM 委员会(技术/基本面/情绪/空头 + 主席回应空头),结构化输出全 clamp。
- ✅ 委员会**宏观感知**:大盘 regime(SPY vs 200SMA)作 advisory 背景喂进 prompt。
- ✅ 委员会**决策校准**:买需具体理由、hold 合法、置信度用满区间、空头有分量。
- ✅ 服务端股数计算 + RiskGate(单票/总仓上限、日亏熔断、冷却、新开仓上限、stale-quote)。
- ✅ 四种模式(advisory / semi_auto / full_auto / 回测),mode 唯一真相在 DB。
- ✅ 每日交易循环编排:screen→committee→gated 下单→撮合;容量预过滤省 LLM。
- ✅ 模拟盘 PaperBroker(下一开盘价成交,盘前回退最近收盘),DB 资金账本。
- ✅ 实盘富途 OpenD 适配器(REAL 硬门,默认模拟盘)。

### 评测体系(M5,本项目的重点投入)
- ✅ **决策记分卡** `GET /decisions/scorecard`:动作分布、置信度直方图、per-mode、闸门状态、诊断 flags。
- ✅ **前瞻收益记分卡** `GET /decisions/forward-returns`:按 action×置信度分桶的实际收益 + 命中率。
- ✅ **诚实门控**:pending(横期未到)/unpriced(无行情)与 matured 严格分开、绝不当 0;置信度→收益的显著性**按决策天数**(非行数)做保守 t 检验;同一置信度值主导时打 caveat。
- ✅ **历史回放评测**(`scripts/replay_eval.py`):在独立评测库上重放 screen→committee,不碰下单路径;`--hold` 测卖出、`--end-date` 跨行情区间;自披偏差(基本面非 PIT、幸存者偏差等)。
- ✅ **决策落 `held` 字段**:记分卡能区分"选择不卖"与"结构上不可能卖"。

### 看盘 / UI(M4 + M6)
- ✅ 11 页 Web UI:Dashboard / Signals / Picks / Watchlist / Stock / Orders / Backtest / Execution / Memory / History / Settings。
- ✅ 个股详情:曲线、指标、新闻、基本面、AI 委员会分析、历史决策、加入自选。
- ✅ 任意代码搜索(NavBar);无效代码优雅 404。
- ✅ AI 荐股 Picks:量化+委员会排序;**浏览器缓存秒开**;**每行可展开看完整四角色理由**(零额外 LLM)。
- ✅ 自选股 Watchlist(增删 + 实时涨跌)。
- ✅ 未实现盈亏 mark-to-market;大盘 regime 横幅(共享组件,Dashboard+Picks)。
- ✅ 业绩追踪 History:胜率、累计盈亏曲线、逐笔复盘、记分卡面板。

### 记忆 / 知识
- ✅ 结构化知识库 + 交易复盘写回 + 自主因子挖掘(证据门槛,多数被证伪)。

### 安全 / 质量
- ✅ 资金路径正确性审查(红线 1/3/4 逐条核验);修复"环内撮合跨标的误杀挂单"(Finding B);遏制性不变量钉死超限买/超卖(Finding A)。
- ✅ 统计核心正确性审查(Pearson/t 检验/分桶/未来函数/分母 全部核验);修复 inf 序列化 500。
- ✅ 785 后端离线测试。

---

## 进行中 / 计划(M8–M9)

### 🟡 M8:让学习闭环真正产生信号(机制已通电 + 已评测,学习效应待攻)
**2026-08 进展**:
- **机制通电 ✅**:`scripts/replay_loop.py` 在隔离库按历史推进完整 `run_trade_cycle`,量产平仓往返 → trade_review 进记忆(实测 25 天出 2 笔往返 2 条复盘,fail-safe 0%)。
- **置信度→收益已测**:三区间干净重跑够样本(39 买入/22 决策日)→ **不显著**(见 PROGRESS 2.4)。
- **学习效应已测**:`scripts/learning_ab.py` 做 DiD → **DiD≈0**,委员会对自己的具体复盘几乎无响应(见 PROGRESS 2.6)。
- **配套**:回放工具加了 Gemini 探活 + fail-safe 熔断(防配额耗尽产污染数据)。

**剩余**:样本量仍小(机制探针非结论);要下强结论需更长窗口/更高换手批量回放。但**关键发现已把重心推向 M9**(下)。

### 🔜 M9:让委员会真正用上自己的复盘(被 M8 的发现重新定义)
- **重新定义**:M8 证明"把该股历史战绩喂进委员会"**已经做到了**(memory_context 含 2000+ 字复盘),瓶颈**不是喂,是委员会没有有效权衡它**(DiD≈0)。所以 M9 的真正工作是**改 prompt/框定**,让委员会**显式对该票自己的亏损复盘作出反应**(如"该票上次我们亏了 11.8%,买入需额外理由"),再用 `learning_ab` 量 DiD 是否转负 —— 有了这把尺子,这是可迭代闭环。
- 扩充因子目录 + 继续证据门槛挖掘。
- 把评测结论更多地接进 UI 决策点(已起步:Picks 的 regime 提示)。

---

## 已知问题 / 技术债(不阻塞,已记录)

| 项 | 严重度 | 说明 |
|---|---|---|
| Finding A(shares 契约) | 中(**已遏制**) | `submit_decision` 信任调用方 shares,仅靠闸门/撮合兜住;字面红线#2 未在函数内强制。已用回归测试钉死遏制性;彻底改需重构 sizing 契约,风险大于收益,暂缓。 |
| Finding C(futu-only) | 低 | 券商 submit 抛异常时可能留下 stranded APPROVED 单;默认 paper 后端不触发。 |
| 学习闭环:委员会对复盘无响应 | 中 | 机制已通电(平仓→复盘→记忆),但 DiD≈0 —— 委员会没权衡自己的复盘。是 M9 的核心工作(prompt/框定)。 |
| 评测样本量 | — | 现有结论多来自单一或少数行情区间,是**弱证据**;跨区间验证已作为方法论固化。 |

---

## 路线图原则(团队约定)

1. **测量先于优化**:改委员会/策略前,先能量出改动的效果(记分卡 + 回放 A/B)。
2. **跨区间才算数**:任何"某类决策好/坏"的结论,至少跨一个反向行情区间验证,否则视为对行情过拟合。
3. **安全代码一次一个改动**:资金路径的改动必须 TDD、最小化、全测试绿;contained 的字面偏差优先用"钉不变量"守而非"改契约"。
4. **诚实优先**:评测宁可报"样本不足/不显著",不把弱证据包装成结论。
